# Rust
/helper/target/
**/*.rs.bk
# NB: Cargo.lock IS committed — it pins exact dependency versions, which the
# reproducible-build / binary-provenance story depends on.

# Lavish review artifacts
/.lavish/

# OS
.DS_Store
